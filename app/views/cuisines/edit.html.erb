<div class="container pt-4 pb-3">
	<h3> Editar tipo de Cozinha </h3>
		<%= form_for @cuisine do |f| %>
	</div>
	<div class="form-group">
		<%= f.label :cuisine_id, 'Cozinha' %>
		<%= f.collection_select :cuisine_id, @cuisines, :id, :name, {}, {class: "form-control"} %>
	</div>
	</div>
	<%= f.submit 'Enviar' %>
	<% end %>
</div>
